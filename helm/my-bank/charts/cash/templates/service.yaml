apiVersion: v1
kind: Service
metadata:
  name: cash
  labels:
    {{- include "cash.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
  selector:
    {{- include "cash.selectorLabels" . | nindent 4 }}