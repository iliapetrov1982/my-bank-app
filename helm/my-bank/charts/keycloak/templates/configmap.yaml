apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-realm
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
data:
  realm-export.json: |
    {{- .Files.Get "realm-export.json" | nindent 4 }}